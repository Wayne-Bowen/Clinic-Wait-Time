// ============================================================
// Fill these in from your Supabase project:
// Project Settings > API > Project URL / anon public key.
// The anon key is safe to expose in frontend code — it has no
// power on its own because of the RLS policies in sql/schema.sql.
// ============================================================
const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";

// The real, deployed URL of patient.html — used to build the QR code
// link shown at check-in. Update this once you've hosted the app
// (e.g. on Netlify/Vercel/GitHub Pages free tier).
const PATIENT_PAGE_URL = "https://your-deployed-site.example.com/patient.html";
